{% extends 'YodaEventBundle:Template:base.html.twig' %}

{% block title %} Festa{% endblock %}

{% block body %}
    <h2>{{event.name}}</h2>
    <ul>
        <li>{{ event.location }}</li>
        <li>{{ event.time|date('d/m/Y H:i') }}</li>
        <li>{{ event.details }}</li>
    </ul>

{%  endblock %}